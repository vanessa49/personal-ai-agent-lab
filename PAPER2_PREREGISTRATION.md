# 论文 2（行为）预注册 v1（已锁定 2026-07-04）

> 状态：**锁定 v1**（task #23 / #17）。假设 + 分析 + 确认集在看确认性数据之前写死。
> 确认集 ID 已冻结于 `eval_pipeline/data/blind_confirmatory_manifest.json`（70 条 fresh open 项，仅 ID、未看内容）。
> 关键诚实点：现有 40 条盲评是**探索性**（已看过），不作确认性证据。
> **重要现实**：fresh open 项池只有 70 条（held-out 共 549，open 仅 110，去掉探索 40 剩 70）→ 盲评确认性**功率不足**，故**主轴改为客观指标④(n=549)，盲评降为欠功率的人类锚点**（见 §5）。

---

## 1. 背景与已知（探索阶段）

- 论文 1 §4.4：trajectory-SFT 在下一步语义相似度上不优于 base；可控性有差异。
- 探索性盲评（n=40，已看）：trajectory 40% vs base/qa 各 30%；单被试随机化检验 one-sided **p=0.23**（不显著）。
- 质性编码（16 例）：胜因集中在连续性/约束保真/意图交付，非答案正确性。

这些是**生成假设**用的探索结果，不作为确认性证据。

## 2. 假设

- **H1（主）**：在风格/动态类客观指标上，trajectory-SFT 的续写比 base 与 qa-SFT 更接近该用户的真实续写分布。
- **H1b（次）**：第一人称盲评中，trajectory 被选为"最像我下一步"的比例 > 1/3（随机基线）。
- **H0**：三种表示在上述指标上无差异（模型身份与偏好/接近度无关）。
- **可证伪**：若主指标上 trajectory 的 CI 覆盖 base 且盲评随机化检验 p≥0.05，则不支持 H1（如实报负结果）。

## 3. 结果指标（锁定）

**主指标（客观、零评审、大 n = 全部确认集）**：
1. **多步轨迹连贯性**：自回归生成 t+1..t+3 的 Δcos 分布与真实 held-out 轨迹的 KS 距离（越小越像）。trajectory vs base vs qa。
2. **风格嵌入距离**：生成续写到"该用户续写风格分布"质心的 bge-m3 距离，trajectory vs base。

**次指标（人类锚点）**：
3. 第一人称盲评胜率（新确认集），SCED 随机化检验（`blind_randomization_test.py`）。
4. LLM-judge 全量胜率 + 与人工的一致性（`llm_judge_pairwise.py`，Cohen's κ 或 % agreement）。

**下限锚点**：可控性（relation token 命中）；传统下一步相似度（预期打平，作对照）。

## 4. 确认集与锁定规则（已冻结）

- **客观指标④（主轴）**：用**全部** temporal held-out，n=549——不受 open 项稀缺限制。
- **盲评①/②（次轴/锚点）**：确认集 = **全部 70 条 fresh open 项**（held-out open 共 110，去掉探索 40 → 70）。
  - **取全部、不再抽样** → 无 cherry-picking；ID 已冻结于 `blind_confirmatory_manifest.json`（2026-07-04，仅 ID）。
  - 续写在 #4 重训后用新 v_smart8 生成；生成前**不看内容**。
- LLM-judge③：全 549 held-out。
- 确认集在人工盲评完成前**不做任何探索性查看**。

## 5. 样本量 / 功率（诚实版）

- 盲评池只有 70（open 项稀缺），**功率不足**：
  | 真实率 | n=70 单侧功率(α=.05) | 拒绝阈 |
  |---|---|---|
  | 0.40（探索估计） | **0.27** | ≥31/70 |
  | 0.45 | 0.59 | ≥31/70 |
  | 0.50 | 0.86 | ≥31/70 |
  | 0.55 | 0.97 | ≥31/70 |
- **结论**：n=70 只有当真实风格效应 ≥0.50 才有像样功率；≈0.40 基本测不出。故**盲评不作主证据**，只作方向性锚点，如实报 p + CI。
- **主证据 = 客观指标④**（n=549，bootstrap CI，无功率瓶颈）。
- **可选扩池（须另行预注册）**：若要提升盲评功率，用 `extract_heldout.py` 从测试期对话多抽 held-out 项 → 重分类 → 追加 fresh open 项到 150+，同样只锁 ID 不看内容。主轴不依赖此扩池。
- 停止规则：达预定集合才检验，**不做窥探式提前停**。

## 6. 分析计划（锁定）

- 盲评：SCED 随机化/精确二项检验 vs 1/3，报 one-sided p + Wilson 95% CI（脚本已定）。
- 客观指标：bootstrap 95% CI + trajectory−base 的配对差，KS 报 D 值。
- LLM-judge：全量胜率 + 人机一致（κ）；judge 测"像 gold"、人工测"像我"，二者收敛才算证据。
- 多重比较：主指标 2 项，Holm 校正。
- 全部只报聚合数（延续 privacy-by-design，不发语料/模型）。

## 7. 外部效度（⑤，非确认性）

合成虚构人格语料 + 多评审，作为"方法在别的轨迹上也区分"的可公开复现探针，独立于上面的单被试确认性检验。

## 8. 时间线依赖

依赖 #14 reverse 数据 + #4 重训（客观指标要在修好的模型上算）。盲评确认集可先抽先锁。
