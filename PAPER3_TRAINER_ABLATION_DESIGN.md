# 论文 3（训练器）实验设计：为什么标准 LoRA 没学到 trajectory 结构

> 状态：设计草案（task #11）。跑之前依赖 #14（reverse 重生成数据）+ #4（能跑通一条 QLoRA）。
> 定位：论文 1 证明 QA vs trajectory 是不同的**数据表示**；论文 3 问的是——既然表示有结构价值，**现有训练范式为什么没把它变成行为差异**？

---

## 1. 研究问题与假设

**观察（已有证据）**：
- 论文 1 §4.4：trajectory-SFT 在 held-out 下一步预测上**不优于 base**；只在可控性（relation token 命中）上有差异。
- RAG 基线同样打平 base（#15）——检索不是答案。
- `time-decay-weight-not-in-loss`：LLaMA-Factory 的 alpaca 根本不消费 per-sample weight；默认 `shuffle=True` + 逐样本交叉熵。

**核心假设 H0**：标准 SFT（打乱样本 + 逐样本 next-token loss）是**顺序无关**的，它把 trajectory 当成一堆独立的 (context→next) 对来学，因此**结构上无法利用** state_t→state_{t+1}→… 的高阶依赖。结构价值存在于数据里，但训练目标把它平均掉了。

**推论**：如果 H0 成立，那么**让训练过程"看见"序列结构**的干预（保序、按 session 组 batch、序列打包、链级损失）应当在**对序列结构敏感的指标**上带来增益，即使传统下一步相似度仍然打平。

**反假设 H1**（也必须诚实检验）：也许结构差异根本无法被 7B+LoRA 在这个数据量上转成行为差异，任何训练干预都无效。若 H1 成立，这本身是有价值的负结果（表示层的结构 ≠ 可学习的行为），要如实报告。

---

## 2. 实验框架

**因变量**：模型行为（见 §4 指标）。
**自变量**：**训练过程**（每个消融只改一件事）。
**严格固定（控制）**：
- 同一份数据（#14 的 reverse 重生成、零泄漏 temporal 切分的 train 侧）；
- 同一 base（Qwen2.5-7B-Instruct）、同一 LoRA rank/alpha/dropout/lr/epoch/量化；
- 同一评测协议（format-matched held-out，见论文 1 §4.4 与 `run_cloze_fair.py`）；
- 同一随机种子（除"样本顺序"消融本身）。

只让训练过程变，其余全锁死——这样任何行为差异都只能归因于训练范式。

---

## 3. 消融矩阵（每条 = 一次 QLoRA 运行）

| # | 消融轴 | 对照 A（标准） | 对照 B（结构感知） | 隔离的机制 |
|---|--------|----------------|--------------------|------------|
| A1 | 样本顺序 | `shuffle=True` | 按对话/轨迹**保序**（shuffle=False + 保序数据） | 呈现顺序是否重要 |
| A2 | batch 组成 | 随机 batch | **session batching**：一个 batch 内样本同源对话 | 同一步梯度里保留一条轨迹 |
| A3 | 序列打包 | 独立样本 | **打包整条轨迹**进一个序列（state_t→t+1→t+2 一次前向，跨步 attention） | 把高阶依赖直接暴露给模型 |
| A4 | 损失目标 | 逐步 next-token | + **链级目标**：用 iteration_final 边，从 root 直接监督收敛态 | 监督整条修正弧而非单步 |
| A5 | 逐样本损失加权 | 均匀 | **per-sample loss 权重**（时间衰减 / edge 类型，= 方案B/#5） | 权重真进 loss（对比方案A重采样） |
| A6 | 课程 | 随机 | **curriculum**：短链→长链，或旧→新（贴合认知演化时间轴） | 顺序结构 + 难度/时间课程 |

**基线锚点**：base（不训）、标准 mixed-SFT（= 现在的 v_smart8，A 列全默认）。所有 B 列都与这两个锚点比。

**实施成本分级**（8GB 4060，逐条 QLoRA≈数小时）：
- 低（改配置/数据顺序）：A1、A6。
- 中（自定义 sampler / collator）：A2、A3。
- 高（改 SFT trainer 的 loss）：A4、A5——与 #5 共用那次 trainer 打补丁。

> 建议先跑**最高信息量、最低成本**的一条做可行性探针：**A3 序列打包**——它是"把高阶依赖喂给模型"最直接的操作，若连它都不动指标，H1 的可能性大增，能早点止损。

---

## 4. 指标：要对"序列结构"敏感（这是关键）

传统下一步相似度已知打平（论文 1），所以论文 3 必须引入**能测出动态的指标**，否则会重复 null 结果：

1. **多步轨迹连贯性（核心新指标）**：给 state_t，自回归生成 t+1→t+2→t+3，计算生成轨迹的 Δcos 分布，与该用户 held-out 真实轨迹的 Δcos 分布做 KS 距离。**这直接测模型有没有学到论文 1 说的那种"动态"**。标准 SFT 若只学到单步，多步会漂/塌；结构感知训练应更接近真实 Δcos 分布。
2. **长程依赖（iteration_final）**：在 root→final 对上，模型从 root 预测收敛态的相似度，是否高于 base / 高于标准 SFT。A4 直接针对这个。
3. **可控性**：relation token 命中率（论文 1 已有），作为"至少学到接口"的下限锚点。
4. **传统下一步相似度**：仍报（预期打平），作为"结构感知训练没有牺牲基础能力"的对照。

> 指标 1 是论文 3 的命门：它把"trajectory 保留 dynamics"这句话变成可测量、可对比训练范式的量。可复用论文 1 的 Δcos/KS 代码（`evaluate_cognitive_graphs.js` / `Δcos` 逻辑）。

---

## 5. 预期结果形态与叙事（两种都能成文）

- **若 B 列在指标 1/2 上优于标准 SFT**：论文 3 = "trajectory 结构可学，但需要顺序感知/链级的训练过程；标准 shuffle+逐样本 SFT 是瓶颈"。给出把结构变行为的**配方**。
- **若 B 列全部打平（H1）**：论文 3 = "表示层的结构差异在当前 7B+LoRA+数据量下不足以转成行为差异"——诚实负结果，缩小了"数据表示决定论"的边界，仍有价值。

无论哪种，都比"再证一次 trajectory 更好"新，因为它回答的是**机制**问题。

---

## 6. 落地顺序（增量、可止损）

1. 先等 #14 reverse 数据 + #4 跑通标准 mixed-SFT（锚点）。
2. 实现指标 1（多步连贯性）评测——**先于任何消融**，因为没有它消融无意义。
3. 跑 **A3 序列打包**（探针）。动 → 继续；不动 → 重估 H1、先跑 A1/A6 低成本项确认。
4. 与 #5（方案B per-sample loss）合并那次 trainer 打补丁，一起做 A4/A5。
5. 汇总成消融表 + 多步连贯性对比图。

---

## 7. 风险与诚实边界

- **算力**：8GB 卡，~10 次 QLoRA + 多步评测，串行要排期；OOM 时 A3 打包的 cutoff_len 需上调，可能要降 rank/batch。
- **n=1 语料**：所有结论仍是单被试，泛化留给多被试（与论文 2 的边界一致）。
- **别过度声称**：多步连贯性用同一被试的 held-out 真实轨迹做参照；报 KS/CI，不吹"更聪明"。
- **与论文 1 的关系**：论文 3 用的是同一份私有语料 + 同样 privacy-by-design；不发模型/语料，发训练配方 + 聚合指标（延续现在的做法）。
